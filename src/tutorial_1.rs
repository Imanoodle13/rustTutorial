fn main() {
    println!("Hello, world!");
}

/*
COMPILE AND RUN YOUR RUST CODE:
1. Set up the following in Cargo.tml:
    [[bin]]
    name = "{program_name}"
    path = "src/{program_name}.rs"
2. `cargo build`:
    Compiling rustTutorial v0.1.0 (C:\{directories}\{program_name})
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.24s 
3. `cd target`
4. `cd debug`
5. ./{program_name}
*/